<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item android:id="@+id/btnHome"
            android:title="Home"
            android:icon="@drawable/ic_home"/>

        <item android:id="@+id/btnSavedPlaces"
            android:title="Saved Places"
            android:icon="@drawable/ic_custom_point"/>

        <item android:id="@+id/btnSettings"
            android:title="Setting"
            android:icon="@drawable/ic_settings"/>
    </group>

    <item android:title="">
        <menu>
            <item
                android:id="@+id/btnHelp"
                android:icon="@drawable/ic_info"
                android:title="Help"/>

            <item
                android:id="@+id/btnInof"
                android:icon="@drawable/ic_info"
                android:title="About"/>
        </menu>
    </item>


</menu>